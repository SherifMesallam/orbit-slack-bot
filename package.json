{
  "name": "deeporbit-slack-bot-refactored",
  "version": "1.0.0",
  "description": "Refactored Slack bot integrating with AnythingLLM and GitHub.",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "validate-config": "node -e \"import('./src/config.js').then(c => c.validateConfig()).catch(e => { console.error('Config validation failed:', e); process.exit(1); })\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@google/generative-ai": "^0.15.0",
    "@octokit/rest": "^20.0.1",
    "@slack/events-api": "^3.0.1",
    "@slack/web-api": "^7.0.2",
    "axios": "^1.6.2",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "form-data": "^4.0.0",
    "morgan": "^1.10.0",
    "node-fetch": "^3.3.2",
    "pg": "^8.11.3",
    "redis": "^4.6.11"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "Rocketgenius",
  "license": "ISC"
}
